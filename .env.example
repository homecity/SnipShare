# SnipShare Environment Variables
# Copy this file to .env.local for local development

# Cloudflare D1 Database (configured in wrangler.toml for production)
# For local development with wrangler, D1 is auto-provisioned

# Application URL (used for SEO metadata)
NEXT_PUBLIC_APP_URL=https://snipshare.pages.dev

# Rate Limiting
# RATE_LIMIT_MAX_REQUESTS=10    (default: 10 per window)
# RATE_LIMIT_WINDOW_MS=60000    (default: 60 seconds)
